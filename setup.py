from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='creating a algorithm which can estimate the change in color at different concentration and at different time instances.',
    author='Aryan Raj Saxena',
    license='',
)
